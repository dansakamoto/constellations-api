[project]
name = "parallax"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "astroquery>=0.4.11",
    "fastapi[standard]>=0.139.0",
    "python-dotenv>=1.2.2",
    "redis>=8.0.1",
]
